function watch () {
  if ! type watch &>/dev/null; then
    while true; do
      clear
      echo "Watching (1 secs.) command: " "$@"
      "$@"
      sleep 1
    done
  else
    `which watch` $*
  fi
}

