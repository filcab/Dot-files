function watch () {
  if type watch | grep "is a shell function" &>/dev/null; then
    while true; do
      clear
      echo "Watching (1 secs.) command: " "$@"
      "$@"
      sleep 1
    done
  else
    `whereis watch` $*
  fi
}

