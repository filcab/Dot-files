set nocompatible       " Put in VIM mode, not vi...

syntax on              " Syntax highlighting is on by default.

set ruler              " Displays line number, column number, and %-location of
                       " cursor in file (on bottom of screen)

set showmatch          " When a bracket is inserted, briefly jump
                       " to the matching one.
set matchtime=1        " Tenths of a second to show the matching paren,
                       " when 'showmatch' is set




